services:
  server:
    build:
      context: .
    env_file:
      - .env
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
    ports:
      - "${PORT:-9180}:9180"
    volumes:
      - ./data/db:/usr/src/app/.db
      - ./data/uploads:/usr/src/app/uploads
    restart: unless-stopped
